{% load static %}
<header>
    <!-- Intro settings -->
    <style>
        #intro {
            /* Margin to fix overlapping fixed navbar */
            margin-top: 58px;
        }
        
        @media (max-width: 991px) {
            #intro {
                /* Margin to fix overlapping fixed navbar */
                margin-top: 45px;
            }
        }
    </style>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark  bg-gradient bg-info fixed-top shadow border-bottom">
        <div class="container">
            <!-- Navbar brand -->
{#        class="m-12 l-3 padding-2x logo"#}
            <a class=" navbar-brand padding-2x logo" href="{% url 'home-page' %}">
                <img src="{% static 'images/logo1.png' %}"  alt="{{ MEDIA_URL }}" loading="lazy" class=" px-1 py-1" style="margin-top: -3px;" />
            </a>
            <button class="navbar-toggler" type="button" data-mdb-toggle="collapse" data-mdb-target="#navbarExample01" aria-controls="navbarExample01" aria-expanded="false" aria-label="Toggle navigation">
      <i class="fas fa-bars"></i>
    </button>
            <div class="collapse navbar-collapse" id="navbarExample01">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="topNav">
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{% url 'home-page' %}">Home</a>
                    </li>
                    {% if not user.id %}
                    <li class="nav-item">
                        <a class="nav-link text-dark px-4" aria-current="page" href="{% url 'find-trip-page' %}">Find Trip</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark px-4" aria-current="page" href="{% url 'scheduled-trips-page' %}">Scheduled Trips</a>
                    </li>
                        <li class="nav-item">
                        <a class="nav-link text-dark px-4" aria-current="page" href="{% url 'pay' %}">Pay Booked</a>
{#                    </li>#}
{##}
{#                        m-pesa token#}
{#                           <li class="nav-item">#}
{#                        <a class="nav-link text-dark px-4" aria-current="page" href="{% url 'token' %}">Token</a>#}
{#                    </li>#}

                    {% endif %}
                    {% if user.id %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{% url 'category-page' %}">Categories</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{% url 'location-page' %}">Trip Locations</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{% url 'bus-page' %}">Vehicles</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{% url 'schedule-page' %}">Trip Schedules</a>
                    </li>
                     <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{% url 'booking-page' %}">Booking</a> 
                    </li>
                    {% endif %}
                </ul>

                <ul class="navbar-nav d-flex flex-row">
                    {% if user.id %} {% if user.profile.user_type == 1 %} {% endif %}
                    <li class="nav-item me-3 me-lg-0">
                        <a class="nav-link" href="{% url 'profile' %}">
                            <span class="me-1"><img src="{% static 'images/default-avatar.png' %}" alt="User Avatar" id="user-avatar" class="img-thumbnail rounded-circle"></span>Hello, {{ user.first_name }}
                        </a>
                        <!-- <a class="nav-link" href="{% url 'logout' %}">Logout</a> -->
                    </li>
                    <li class="nav-item me-3 me-lg-0">
                        <a class="nav-link" href="{% url 'logout' %}">Logout</a>
                    </li>
                    {% else %}
                    <li class="nav-item me-3 me-lg-0">
                        <a class=" btn btn-primary m-2" href="{% url 'login' %}">Admin</a>
                        
                    </li>
{#                     <li class="nav-item me-3 me-lg-0">#}
{#                        <a class=" btn btn-primary m-2" href="{% url 'register-user' %}">Signup</a>#}
{#                    </li> #}
                    {% endif %}
                  
                </ul>
            </div>
        </div>
    </nav>
    <!-- Navbar -->

    <!-- addons -->
    <div id="intro" class="pt-2 text-center bg-light">

    </div>
    <!-- Addons  -->
</header>
<script>
    $(function() {
        $('#topNav .nav-link').each(function() {
            var current = '{{ request.get_full_path | urlencode }}'
            if (current === $(this).attr('href')) {
                $(this).parent().addClass('active')
            }
        })
    })
</script>